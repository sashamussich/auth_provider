<h1>Welcome to Sasha's SSO Provider</h1>
<% if current_user %>
	<div class="text-center">
		<h1> <%= current_user.name %></h1>
		<!-- <h4> Logged in with <strong><em><%#= @auth['provider'].capitalize %></em></strong> </h4>
		<%#= image_tag(@auth['info']['image']) %> -->
	</div>
<% end %>